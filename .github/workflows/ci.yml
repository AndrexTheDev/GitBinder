name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  test:
    name: Test & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      # Build *before* testing: two assertions in tests/deploy.test.js
      # ("the built site is entirely self-contained" and "…ships the deploy
      # files…") skip themselves when dist/ is absent. Testing first meant they
      # never ran in CI, silently. Build first so the gate is whole. (F-01)
      - name: Build the static bundle
        run: npm run build

      - name: Lint the module graph (no unused exports policy is manual)
        run: npm test

      # Kept as a plain artifact for inspection. `upload-pages-artifact` is
      # for GitHub Pages, which this project does not use — Cloudflare Pages
      # builds `dist/` itself, so the job above is the real gate.
      - name: Upload the bundle
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          retention-days: 7
